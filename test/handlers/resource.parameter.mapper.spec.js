var assert = require('chai').assert;
var sinon = require('sinon');

var ResourceParameterMapper = require('../../src/handlers/resource.parameter.mapper');

describe('Maps the query string and url parameters for a given resource to JSON object attributes', function() {
    beforeEach(function() {
    });

    afterEach(function() {
    });

    it('testToResourceDaoQueryObject(Object resourceAttributeUrlParameterMap) - should return a map a resource dao could use to query in memory JSON resource db.', function() {

    });
});